// === DRAG & DROP HANDLER - UNINOTE ===

import { STATE } from '../../config/state.js';

export class DragDropHandler {
  static init() {
    // El código de drag & drop está actualmente en uninote-legacy.js
    // Para migrar: mover las funciones de las líneas 2746-2803
    console.log('DragDropHandler: Pendiente migración desde legacy');
  }

  static handleDragStart(event) {
    // Implementación pendiente
  }

  static handleDragOver(event) {
    // Implementación pendiente
  }

  static handleDrop(event) {
    // Implementación pendiente
  }

  static handleDragEnd() {
    // Implementación pendiente
  }
}
