// === TEXT FORMATTER - UNINOTE ===

import { STATE } from '../../config/state.js';

export class TextFormatter {
  static applyStyle(command, value = null) {
    // Código actual en legacy líneas 1356-1368
    console.log('TextFormatter: Usando versión legacy');
  }

  static changeFontSize(direction) {
    // Código actual en legacy líneas 1370-1393
    console.log('TextFormatter: Usando versión legacy');
  }

  static updateToolbarState() {
    // Código actual en legacy líneas 1339-1346
    console.log('TextFormatter: Usando versión legacy');
  }
}
