// === LINKIFIER - UNINOTE ===

export class Linkifier {
  static linkify(element) {
    // Código actual en legacy líneas 1235-1271
    // Convierte URLs en links clickables
    console.log('Linkifier: Usando versión legacy');
  }
}
