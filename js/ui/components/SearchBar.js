// === SEARCH BAR - UNINOTE ===

import { STATE } from '../../config/state.js';

export class SearchBar {
  static handleSearch() {
    // Código actual en legacy líneas 1894-1903
    console.log('SearchBar: Usando versión legacy');
  }
}
