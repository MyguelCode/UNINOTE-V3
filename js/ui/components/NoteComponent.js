// === NOTE COMPONENT - UNINOTE ===

import { STATE } from '../../config/state.js';

export class NoteComponent {
  static create(parentList, afterElement = null, shouldFocus = false, data = {}) {
    // Código actual en legacy líneas 1438-1516
    console.log('NoteComponent: Usando versión legacy');
    return null;
  }

  static render(noteLi) {
    // Renderizar estado de nota
  }
}
