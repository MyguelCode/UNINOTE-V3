// === TABS BAR - UNINOTE ===

import { STATE } from '../../config/state.js';

export class TabsBar {
  static render() {
    // Código actual en legacy líneas 2266-2279
    console.log('TabsBar: Usando versión legacy');
  }
}
