// === PROMPT MODAL - UNINOTE ===

export class PromptModal {
  static show(title, message, options = {}) {
    // Código actual en legacy líneas 1134-1162
    return new Promise(resolve => {
      console.log('PromptModal: Usando versión legacy');
      resolve(null);
    });
  }
}
