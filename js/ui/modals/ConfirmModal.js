// === CONFIRM MODAL - UNINOTE ===

export class ConfirmModal {
  static show(title, message, options = {}) {
    // Código actual en legacy líneas 1175-1193
    return new Promise(resolve => {
      console.log('ConfirmModal: Usando versión legacy');
      resolve(false);
    });
  }
}
