// === ALERT MODAL - UNINOTE ===

export class AlertModal {
  static show(title, message) {
    // Código actual en legacy líneas 1123-1132
    return new Promise(resolve => {
      console.log('AlertModal: Usando versión legacy');
      resolve();
    });
  }
}
