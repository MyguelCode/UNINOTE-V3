// === IMPORT MODAL - UNINOTE ===

export class ImportModal {
  static show(importType) {
    // Código actual en legacy líneas 1195-1233
    return new Promise(resolve => {
      console.log('ImportModal: Usando versión legacy');
      resolve('cancel');
    });
  }
}
